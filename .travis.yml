language: node_js
sudo: required
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
node_js:
  - "7"
before_install:
  - sudo apt-get install yarn
  - yarn setup
install:
  - webpack
script:
  - yarn test
  - yarn start
